#include <stdio.h>

/**
 * main - base ten (10) numbers
 *
 * Description: return base 10 single digit numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)

	{
		printf("%.1d", a);
	}

	return (0);
}
