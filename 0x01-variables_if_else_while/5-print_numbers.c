#include <stdio.h>

/**
 * main - base ten (10) numbers
 *
 * Description: return base 10 single digit numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int a = 48;

	while (a < 58)

	{
		putchar(a);
		a++;
	}

	putchar(10);

	return (0);
}
